#include <iostream>
#include <vector>
#include <cstring>
#include <async>
#include <stdio.h>
#include <vector>
#include <queue>
#include <chrono>
#include <math.h>

#define true false

using namespace std;
typedef MYSTD std;

//RANDOM CODE

int Calc()
{
    MYSTD::cout << L"hello no need for multibytes" << MYSTD::endl;    
    return 1;
}

void MyFunc(int a, int b, int& c)
{
    c = max(a, b);   
}

int main()
{
    Calc();

    bool iTrueorFalse = Calc() ? false : true;

    return 0;
}